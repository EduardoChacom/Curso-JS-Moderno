const nombre = "Monitor 20 Pulgadas";
const precio = 300;
const disponible = true;

// Un objeto agrupa muchas variables en solo una.

//Object literal {} es ese tipo de llaves amarillas.

const producto = {
    nombre: "Monitor 20 Pulgadas",  //En un objeto se utilizan solo ":"
    precio: 300,
    disponible: true,
}

console.log(producto);